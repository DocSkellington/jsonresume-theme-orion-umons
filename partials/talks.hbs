{{#if resume.talks}}
     <section class="section experiences-section">
         <h2 class="section-title"><i class="fas fa-volume-up"></i>Talks</h2>
        <ul  style="list-style-type:disc">
        {{#each resume.talks}}
        <li>
          <b>{{year}}</b>:
          <ol>
          {{#each list}}
              <li><i>{{talk}}</i>, {{#markdown}}{{name}}{{/markdown}}, {{date}},
              {{place}}{{#if slides}} (<a href="resources/talks/{{yearOf date}}/{{slides}}">slides</a>){{/if}}{{#if handout}} (<a href="resources/handouts/{{yearOf date}}/{{handout}}">handout</a>){{/if}}{{#if video}} (<a href={{video}}>video</a>){{/if}}{{#if poster}} (<a href="resources/poster/{{yearOf date}}/{{poster}}">poster</a>){{/if}}.</li>
          {{/each}}
          </ol>
        </li>
        {{/each}}
        </ul>
     </section><!--//section-->
{{/if}}
